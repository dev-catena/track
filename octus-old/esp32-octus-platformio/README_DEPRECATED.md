# ⚠️ Firmware movido

O firmware ESP32 foi movido para:

**`/home/darley/track/esp32-firmware/`**

Use a nova pasta para compilar e fazer upload:

```bash
cd /home/darley/track/esp32-firmware
pio run
pio run -t upload
```

Esta pasta em `octus/` é legada e não deve ser usada.
